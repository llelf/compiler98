module Directory where

import GreenCard

%-#include <sys/stat.h>
%-#include <unistd.h>

%-#define TRUE   1
%-#define FALSE  0

%fun doesDirectoryExist :: FilePath -> IO Bool
%call (filePath fp)
%code
%  struct stat st;
%  int err;
%  err = stat(fp,&st);
%  if ((err==0) && S_ISDIR(st.st_mode)) b=TRUE;
%  else b=FALSE;
%result (bool b)

